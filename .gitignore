# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# Build outputs (bench arrays, trained models, metrics, plots)
outputs/
# The code writes `outputs/`, but the unpacked Kaggle archive lands in
# `output/` (singular) — 240M of runs, so ignore that spelling too.
/output/
bench/
*.npy
*.npz
*.pt
*.pth
*.csv

# Packaging / generated docs
build/
dist/
_build/

# Datasets (download separately; see README)
dataset/
UT_HAR/
NTU-Fi_HAR/

# Reference papers (third-party PDFs — not redistributable)
/paper/

# OS / editor
.DS_Store
.vscode/
.idea/
